# macOS (Apple Silicon) requirements for running DeepFace with TensorFlow-Metal and PyTorch (MPS)
# This file intentionally omits tensorflow-macos/tensorflow-metal and torch (PyTorch metal wheel).
# Install those two packages separately following the instructions in docs/MPS_SETUP.md.

# Tensorflow (compatible versions for macOS Metal - using available PyPI versions)
tensorflow==2.14.0
# tensorflow-metal==1.1.0

# Core Python packages
numpy==1.26.4
pandas==2.0.3
Pillow==10.2.0
opencv-python==4.9.0.80
requests>=2.27.1
gdown>=3.10.1
tqdm>=4.30.0

# Deep learning / model glue (version-pinned to be compatible with tensorflow-macos 2.14.x)
keras==2.14.0
typing-extensions==4.9.0
protobuf==4.23.4
tensorboard==2.14.0

# Torch
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
facenet-pytorch==2.6.0

# Web / API
Flask>=1.1.2
flask_cors>=4.0.1
gunicorn>=20.1.0

# YOLO
ultralytics>=8.2.0

# Optional detectors (installed after torch/tf)
# retina-face, mtcnn and ultralytics may require extra steps; install them after core stack.
mtcnn>=0.1.0
retina-face>=0.0.14
fire>=0.4.0
